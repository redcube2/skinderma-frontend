import HomePage from "@/components/pages/HomePage";

/**
 * Hungarian home page (/hu). Localised metadata is emitted by the root
 * app/layout.tsx generateMetadata for the home route.
 */
export default function Page() {
  return <HomePage locale="hu" />;
}
